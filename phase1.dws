{100×(+/⍵∊'GC')÷⍴⍵}
{((⍳1↑⍴⍺),0)[(⍺⍳⍵)]}
{(⊂[1](0=⍉(⍺∘.|,⍵)))/¨⊂⍵}
{(¯2+○1)×(⍵÷2)*2}
{A←(((⊂⍵)÷⍳⍵), ⍵*0.5)∩⍳⍵ ⋄ B←A[⍋A] ⋄ B[(⌈¯1+0.5+(⍴B)÷2) (⌊1+0.5+(⍴B)÷2)]} ⍝ consider cases like 0 thhat should give 2⍴0
{(∧/'RKR'=⍵∩'RK')∧2|+/(⍵∊'B')/⍳⍴⍵}
{A←⊃,/(+/⍉⍵)(+/⍵)((1↑⍴⍵)/+/,⍵×⊃(∘.=/⍳¨⍴⍵)) ⋄ ∧/A=1↑A}
{mhd←1 60 1440⋄|(+/(⌽,⍺)×mhd[⍳⍴,⍺]) - (+/(⌽,⍵)×mhd[⍳⍴,⍵])} ⍝ index error with 'COOLAPL'
{⌈/(+/¨(⊃¨(+/¨¨1∩¨¨(⊂⍵)÷¨⍵)⊆¨((⍴⍵)⍴⊂((⍴⍵)/1))))} # tentative 'cause it misseda test case, did exactly what i wanted, the problem is unclear.
{a←(⊂⍣(2>|≡,⍵)),⍵⋄(,⍣(2>|≡⍵))(⊃(2=|≡a)/≢a)(⍺)⍴⊃,/(0-⍺)↑¨a}